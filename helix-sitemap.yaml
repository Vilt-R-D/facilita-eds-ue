sitemaps:
  default:
    source: /query-index.json
    destination: /sitemap.xml
    lastmod: YYYY-MM-DD
  alternative:
    source: /query-index.json
    destination: /testsitemap.xml
    lastmod: YYYY-MM-DD